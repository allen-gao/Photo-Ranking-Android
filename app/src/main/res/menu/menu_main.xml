<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.fotagmobile.MainActivity" >

    <item android:id="@+id/load_images"
        android:title="@string/load_images"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/clear_rating"
        android:title="@string/clear_rating"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/clear_all"
        android:title="@string/clear_all"
        android:orderInCategory="100"
        app:showAsAction="never" />
</menu>
